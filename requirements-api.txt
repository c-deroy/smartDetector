# Requirements for the FastAPI serving container only.
# matplotlib, seaborn and streamlit are training/dashboard tools,
# not needed to serve /predict, /predict_simple, /explain etc,
# so they're left out here to keep the Render image smaller and
# reduce RAM usage at runtime. Keep using your full requirements.txt
# for local training and the Streamlit app.

numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
xgboost>=1.5.0
joblib>=1.1.0
imbalanced-learn>=0.9.0
pyod>=1.0.0
fastapi>=0.68.0
uvicorn>=0.15.0
shap>=0.42.0
